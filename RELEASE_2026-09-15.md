# 2026-09-15 修復版：依整體檢測報告逐項修復

對應報告：[GAME_AUDIT_2026-09-15.md](GAME_AUDIT_2026-09-15.md)。本次未改變棋盤與卡牌數值的整體尺度，只修正錯誤與流程卡點。

## 會卡住活動的問題

- **開局檢查含離線玩家**：任何未完成職業選擇的玩家（含離線）都會擋開局並點名；「強制開始」會替所有未完成者補齊真正的職業。遊戲中再送 `startGame` 會被拒絕。
- **人際關係機遇卡**：手機新增「接受／婉拒」卡片；主持人略過或玩家未送出一律視為婉拒，不再自動執行賭注。
- **「這次不旅行」「略過」**：`submitCardDecision` 允許 `null` 的 Id 代表略過，不再被驗證器擋下。
- **臥床玩家**：手機顯示「跳過本回合（臥床判定）」按鈕；主持人另有 `skipTurn` 可跳過離線、發呆或臥床玩家。
- **大螢幕發薪小卡**：6 秒自動收起，並在下一位結算、決策開始或遊戲繼續時自動關閉，不再擋住後續轉場與棋子移動。
- **手機錯誤提示**：遊戲中所有伺服器錯誤以 toast 顯示並自動消失；旅遊／社交失敗原因也會顯示。
- **舊頁面被接手**：伺服器先送 `sessionTakenOver` 再切斷；手機顯示連線橫幅與「重新連線」按鈕，加入畫面以外都會顯示離線狀態。

## 公平性與數值

- 危機池依實際卡片重排：青年期與成家期不再含致死卡（新增測試守住）。
- 現金為負時扣款最多扣到 0，慈善不會倒扣也不會白拿加骰。
- 競標：放棄者不能自己出價；得標金由銀行收取。
- 還款信用加分依還款佔債務比例計算；學貸不占信用額度（銀行與 P2P 同規則）。
- 升遷／減薪卡改由 `salaryMultiplierPending / salaryMultiplierMonths` 在每月結算套用；決策回聲加薪改為永久 `salaryBonus`。
- 第二人生：健康門檻 70、至少結算過一季（6 個月）、玩家間借貸利息不計入被動收入。
- 人生評分淨值含現金、尺度改為 $1,500,000 / 年被動 $750,000 = 100 分；家庭維度三個孩子可滿分。
- 加入房間時忽略客戶端指定職業；加盟主不能重複申請；已故玩家不受市場卡影響，結算沿用死亡當下分數。
- `mk-008` 改為可實作的「法拍屋搶購潮」房地產 +15%；rel-002 同學會改走正式小交易決策；願望清單不再抽到已達成目標；慈善以薪資或被動收入較高者計算。
- 主持人改 FQ 限 1–10。

## 主持人與大螢幕

- 大螢幕、後台、手機控場只在主持人手動暫停時顯示「已暫停」；決策與落格說明期間顯示目前狀態。
- 後台新增「落格說明節奏」：每次手動／5／10／20 秒自動放行（`setReadingAutoContinue`）。
- 大交易本人買不起且全場沒人出得起頭期款時直接略過；本人買不起但他人可能出價時直接開競標。
- 後台錯誤改為頂部紅色提示；刪除房間改為等伺服器確認；舞台事件進行中不再顯示「繼續」；轉職舞台在玩家確認前不能揭曉；季度發薪失敗會顯示。
- 移除當前回合玩家時先算好下一位再移除，輪次不再跳回第一位。
- 大螢幕與舞台 overlay 內容過高時改為可捲動且不裁上緣；迷你地圖改為 24 格。

## 重連

- 重連補送 `crisisNTSkipAvailable`、`fastTrackPartnershipInvitation`；進行中的競標寫入 `gameStateUpdate`，重新整理後可繼續出價。
- 掃到不同房間的 QR 時不再用舊身分自動續玩；瀏覽器殘留的失效 token 不會對新玩家顯示錯誤。
- 結果類卡片（意外支出、危機結果等）不再被無關的決策結束事件清掉。

## 低嚴重度補修（同日第二批）

- 祝賀改為每收到 5 次 NT +1，人脈值維持整數（新增 `congratulationsReceived`）。
- 大螢幕側欄、棋盤棋子、骰子動畫、轉場與發薪小卡統一使用 `playerColors.ts` 色盤，並一律以 `playerOrder` 順序取色。
- 手機行動面板新增「提前還款」（應急、槓桿、玩家借貸、學貸可按 25%／50%／全額）與「退保」；面板的可借額度計算同步排除學貸。
- 移除 `playerRoll` 內已無作用的單格發薪規劃流程（約 200 行），發薪只走季度全體結算。
- 移除伺服器已不發送的前端監聽：`gameClock`、`careerChangeAnnouncement`、`partnershipOpportunity`、`playerStatUpdated`，以及對應的合夥選擇 UI。

## 設計取捨（同日第三批，依審查建議）

- **危機自救**：致死條件成立時先開「危機自救」決策階段（提醒 120 秒）。本人手機顯示需要金額與差額，「行動」面板在此階段開放賣資產與應急借款；主持人繼續後以當下現金重新判定，補不足才出局。內圈危機、外圈危機考驗與疾病危機都適用（`crisisRescueRequired`，重連會補送）。
- **外圈致死疾病卡**：無保險時費用改為淨值 × 30%、下限 $900,000，取代固定 $1.8M／$3M。
- **交易壓力曲線**：投資槓桿借款利率改為應急利率 × 1.25（原 × 0.8）；大交易牌組月現金流整體 +20%。
- **詐騙受害**：改為扣目前現金 50%、上限 $120,000（新增 `cashCostShare` 欄位）。
- **年齡文案**：後台開局日誌改為「全場年齡每輪 +4 歲（個人起步年齡只影響前幾輪顯示）」；邏輯不變。
- **落格說明自動放行預設 10 秒**（主持人仍可改回手動或 5／20 秒）。
- 同一瀏覽器多分頁互蓋身分：依建議不處理。
- 新增測試：自救階段允許本人借款、其他人仍被擋、補不足死亡；費用預覽不改狀態；詐騙比例扣款；槓桿利率；大交易數值。

## 驗收期間追加（2026-09-16）

- **決策卡片時序**：卡片改在落格說明結束、決策階段開始後才送到手機；手機不再因落格說明結束清掉卡片。修正「有決策但沒選項」。
- **送出後自動揭曉**：玩家送出選擇後 1.5 秒自動揭曉（`autoRevealOnSubmit`，預設開，後台可關）；競標仍由主持人結束。

## 驗收期間追加（2026-09-16，第二批）

- **房貸與車貸可提前還款**：職業自帶的房貸、車貸改為「自住房／自用車」資產＋同額負債（房貸 240 期、車貸 60 期本金），淨值不變、不占信用額度；行動面板「提前還款」可還本金，月付等比例下降，還清後月付歸零、房車保留；不可出售。換職業不再重設房貸車貸。
- **股票報酬提高**：定期定額每月增值 0.5% → 0.9%、配息 0.25% → 0.4%（約年化 16%）；股票類交易卡月現金流 ×2。
- **行動按鈕顯示不能選的原因**：出國、聯誼灰掉時直接寫出「健康值未達 50」「本次發薪活動額度已用完」「已婚」「臥床」。
- **金流明細**：手機點「月現金流」顯示收入與支出逐項組成（含乘數與世界事件調整）；點「手頭現金」顯示最近 20 筆現金變動與起始現金。

## 機制改版第一階段（2026-09-16）：每輪開始的全體行動時間

- 每輪擲骰前開「全體行動時間」（`actions` 決策階段，90 秒提醒）：全員同時在手機處理旅遊、聯誼、保險、投資、借還款；每人按「我這輪完成了」，全員完成自動結束，主持人可提前結束。後台可關閉（`setActionPhaseEnabled`）。
- 固定班表職業的活動額度改為每輪 1 次，在行動時間開始時重置。
- 大螢幕顯示每位玩家是否完成；後台與手機控場的結束按鈕顯示完成人數。
- 驗證器把 `emit(event, undefined)` 編成的 `null` 視同無 payload，避免無參數事件被誤擋。

## 機制改版第二階段前半（2026-09-16）：全體同時發薪規劃

- 季度發薪改為一個全體 `payday` 決策階段：所有人同時在手機填規劃，大螢幕顯示已送出人數，全員送出自動結算；主持人可提前以空白方案結束。重複送出只算第一次；離線玩家用空白方案。
- 結算仍依回合順序逐位公布發薪小卡；重連時若尚未送出會補送規劃表單。

## 機制改版第二階段後半（2026-09-16）：計時發薪（混合制）

- 計時器（預設 10 分鐘，後台可調 5／10／15／20 分鐘或改回每三輪）到期後，發薪排在目前玩家行動結束時執行；全場空檔時直接開始；至少要經過一輪。
- 結算月數 = 距上次發薪經過的輪數 × 12；成長週期數 = 經過輪數；發薪表單的健康維護費依週期數計算。
- 大螢幕中央改為「下次發薪倒數」與已累積年數；手機頂欄顯示倒數；後台可看倒數、調整間隔、立即發薪。
- 時鐘在決策、舞台、暫停時凍結，倒數同步凍結。

## 機制改版第三階段（2026-09-16）：65 歲人生轉折

- 第 11 輪完成後，內圈未退休玩家逐位開「人生轉折」舞台，本人在手機選：退休（退休金 = 平均月薪 × E 40%／S 25%／B、I 0%）、當顧問（第二專長 × 300 + 人脈 × 3,000，每輪 HP −5，HP < 50 無收入）、創業（30／75／150 萬，擲骰 ≥ 4 成功，月現金流 = 投入 × 8%，失敗損失一半）、延後一輪（HP −10，限一次）。主持人只能在本人選擇後揭曉。
- 65 歲起高齡支出：HP < 60 每月 +$3,000，HP < 30 再 +$9,000。
- 退休者踩「職涯轉折」格改為一次性顧問案，不再裁員。
- 65 歲後的薪資階段倍率取消，由選擇決定；財務報表與現金流明細顯示退休金／顧問收入。
- 新增單元與 socket 測試：退休金與顧問收入計算、高齡支出、舞台流程（本人選擇、主持人揭曉、延後）。

## 復盤修正（2026-09-16 晚）

- **復盤頁崩潰**：手機「人生分析」與大螢幕「顯示分析」一開就出現「Element type is invalid… GameChart」。原因是圖表元件從 `echarts-for-react/lib/core`（CommonJS）匯入，在 Vite 下拿到的是模組物件而不是元件；改為 `echarts-for-react/esm/core`。
- **發薪紀錄精簡**：復盤的發薪事件改為每結算 12 個月記一筆，年齡依經過輪數往前推（每輪一點），時間軸不再出現同一年齡疊 36 個點、手機紀錄不再有兩百多筆發薪。

## 驗收修正第二批（2026-09-16）

- **手機控場免輸入控制碼**：後台 QR 區新增「手機控場 QR」（含房號與控制碼，主持人自己掃，不要投影）；掃進去自動登入，也會記住控制碼供之後重連。
- **後台玩家列「觸發邂逅」「套用數值」看似沒反應**：原本結果只寫進後台日誌；現在按下後在該玩家底下即時顯示「套用中／已套用／已觸發邂逅／失敗原因」。行動時間中也可觸發邂逅。
- **決策內容全場公開**：大螢幕與後台決策卡列出交易卡條件（頭期款、月現金流、現金與可借額度、危機缺口、慈善／人際說明等），不再只顯示「思考時間」；底部文字改為「內容全場公開，一起討論」。
- **結婚禮金可調**：後台「節奏」區新增結婚禮金輸入，覆蓋所有路徑的禮金；填 0 恢復預設。
- **祝賀金額可選**：$3,000／$7,500／$15,000／$30,000 或自填，現金不足不能送。
- **被動收入加強**：定期定額增值 0.5%→0.9%／月、股息 0.25%→0.4%／月（年化約 16%）；定期定額額度新增 $150,000／$300,000／$750,000；基本投資（定存、債券、REIT 等）每次可買 1／2／5／10 份，月現金流按份數加總。

## 手機新增「脫離內圈還差多少」（2026-09-16）

- 內圈玩家手機在「財務報表」上方多一個可展開面板，標題直接顯示被動收入覆蓋率。展開後有：覆蓋率進度條（標 75%／100%）、有效被動收入的算法（被動 × 財商乘數）與月支出、「平衡人生」「財務突破」兩張路徑卡各還差多少錢與幾項指標、四項人生指標的數值與補法、前置條件，以及最短路徑建議（缺口最小的路徑，列出要多買多少月現金流、最接近達成的指標）。
- 後端在玩家資料加 `secondLifeProgress`，已進外圈為 null；算法與正式資格判定共用同一函式，不會出現「面板說達成、實際沒進圈」的落差。

## 真實人生擬真（2026-09-16 第四階段）

依「遊戲與真實人生落差」評估，一次補齊：
- **時間尺度**：每輪結算 24 個月（可在後台改 12／24／48），房貸車貸在這個尺度下真的還得完。定期定額與基本投資報酬依新尺度下修（DCA 增值 0.6%、股息 0.3%／月；定存 $60、基金 $200、事業 $450／份）。
- **薪資成長曲線**：每輪依階段實質加薪（8%／5%／1%），SK ≥ 60 再 +2%；升遷卡永久 +10%。
- **支出會變**：生活支出與房租每輪 +3% 到 65 歲；子女支出依孩子年齡分段（幼兒／學齡／大學），23 歲獨立歸零；扶養扣除只算未成年。
- **住房決策**：開局租屋，手機「行動 → 住房」可買小宅／三房／透天（頭期款 20%、3% 稅費、30 年房貸年利 2.4%），賣房回到租屋、免資本利得稅。
- **家庭現實**：結婚建立配偶收入（本人薪資 × 0.5–0.9），配偶失業卡、離婚卡（現金 −25%、失去配偶收入、HP −10）；父母重病／長照危機卡（成家、轉型期），人脈 ≥ 5 親友分攤減半。
- **生活方式與健康習慣**：發薪規劃新增節儉／普通／享受、規律運動／普通／熬夜加班，直接影響支出、HP 衰退、薪資與體驗。
- **失業長度依年齡**：裁員 2／4／8 個月，SK ≥ 60 減半。
- **自然壽命**：80 歲起每輪依 HP 判定（4% + (100 − HP) × 0.25%）；不再人人活到 100。
- **其他**：勞健保（薪資 5%）、保費隨年齡上升、高齡醫療 $5,000／長照 $25,000、出售資產課 20% 資本利得稅。
- 手機財務報表與現金流明細列出房租、勞健保、配偶收入、孩子年齡、奉養照護、生活方式倍率；後台可調每輪結算月數。
- 新增 `tests/real-life.test.js`（8 項），既有測試依新規則更新，共 50 項。

## 每個行動都標注效果與價值、每個財務數字都有組成（2026-09-17）

- **行動面板**：旅遊目的地、聯誼、保險、提前還款、定期定額、應急借款、槓桿借款、賣出資產、買房、轉職，點選後先出現「效果與價值」卡（費用、得到什麼、月現金流變化、信用分變化、剩餘現金、附註），確認才執行。數值全部由伺服器依當下狀態算（保費年齡倍率、去過的地方體驗減半、資本利得稅、賣房稅費、還款後月付與信用加分）。
- **發薪規劃**：財商升級、健康投資、進修、人脈、定期定額、基本投資、保險、聯誼、旅遊每一項下方都有「價值：…」說明（例如財商乘數 ×1.05 → ×1.10、每月多多少被動收入；基本投資年化與回本月數；保險抵免哪些危機與節稅）。
- **財務數字**：月總收入、月總支出、淨資產、薪資、被動收入、支出細項、資產與負債清單、能力值、信用分都可點，彈出組成明細：薪資列出起薪 × 年資倍率 × 健康習慣 × 暫時倍率的每一步；淨資產列出現金 + 各資產市值（含成本、增值、連結負債）− 各負債；信用分列出利率／上限分級與加減分規則；能力值說明每個數值影響什麼。

## 其他

- Pre-20：社會階層標籤全中文，點數沒分完不能確認，進修職業清單與實際牌池一致，強制開始說明文字修正。
- 刪除未使用的 `useSocket.ts`、`useGameState.ts`。
- GAME_MECHANICS.md 依程式碼重寫；新增 `tests/audit-fixes.test.js`、`tests/host-flow.test.js`（後端測試共 31 項）。
- 新增 `.claude/launch.json` 供本機一鍵啟動前後端。
