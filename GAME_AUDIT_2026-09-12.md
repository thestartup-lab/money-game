# 百歲人生｜遊戲檢測與優化報告

檢測日期：2026-09-12。受測提交：`0717bf9`。

## 結論

**現有正常路徑測試通過，但尚不適合宣稱已完成正式活動的完整驗收。** 本次在獨立本機伺服器重現了異常請求造成程序退出、決策斷線恢復失效、P2P 借貸帳務不一致等問題。建議在下一場正式活動前先處理阻斷性問題，再做完整多人實機驗收。

正式後端只做唯讀 `/health` 檢查：`ok: true`，版本與本機一致，查詢當時房間數為 0。**所有異常情境均在本機 3307 埠執行；沒有對正式服務送出異常請求、建立玩家或修改房間。** 本次未修改遊戲程式或部署，僅新增本報告。

## 檢測範圍與結果

| 範圍 | 結果 | 證據／限制 |
|---|---|---|
| 既有後端測試 | 通過 | `npm test`：8 項通過、0 失敗 |
| 前端静態檢查 | 通過 | `npm run lint` 無錯誤 |
| 前端正式建置 | 通過、有優化警告 | TypeScript／Vite 成功；大型 bundle 警告 |
| 正式服務存活及版本 | 通過 | 唯讀健康檢查與 `0717bf9` 一致，不等於全部玩法通過 |
| 加入與異常輸入 | 未通過 | 異常加入 payload 可使本機後端 exit code 1 |
| 主持人暫停與操作限制 | 未通過 | 暫停中仍能透過 API 投資並改變現金 |
| 玩家間借貸 | 未通過 | 還清債務後出借方債權仍保留；負數借款亦被接受 |
| 決策途中斷線重連 | 未通過 | 玩家 id 更新，決策仍指向舊 id，新連線提交無回應 |
| 重連身份驗證 | 未通過 | 全新連線只憑名字和房間代碼可接手離線角色 |
| 開局後新玩家加入 | 已重現缺乏限制 | 遊戲開始後仍直接接受新角色，未經主持人審核 |
| 4–6 支真實手機＋大螢幕跑完 20 輪 | 尚未驗證 | 需先修復上述失敗邊界；本次不是完整實機驗收 |
| 外圈、婚姻生育、終局與經典保存的完整交叉情境 | 尚未全部驗證 | 現有測試與程式檢查不足以取代全程實測 |

依完整流程驗證技能的停止條件，確認阻斷邊界後，不繼續把其後的流程當作已驗收。下列是實際重現與獨立程式檢查的結果，兩者分開標示。

## 一、優先修復：已重現的問題

### 1. P1｜異常加入資料可讓整個後端中斷

位置：[socketServer.ts:2434](/Users/lipeter/Documents/money-game/socketServer.ts:2434)。

本機獨立連線傳送空值加入資料後，伺服器直接退出：

```text
TypeError: Cannot destructure property 'playerName' of 'payload' as it is null.
server exit code: 1
```

影響：不是只有一名玩家收到錯誤，而是同一程序上的房間都會中斷。房間目前存在記憶體 Map 中；程序重啟後無法從目前架構恢復同一場進度。Railway 的失敗重啟設定不等於進度恢復。

建議：所有 Socket 入口先做 runtime schema 驗證，包含空值、型別、字串長度、列舉、數值上下界；統一錯誤處理並記錄事件名稱。不要只靠 TypeScript，也不要單純吞掉 uncaught exception 繼續跑可能已不一致的狀態。

### 2. P1｜手機斷線後，正在選的交易不能正常續選

位置：[socketServer.ts:5348](/Users/lipeter/Documents/money-game/socketServer.ts:5348)、[socketServer.ts:5448](/Users/lipeter/Documents/money-game/socketServer.ts:5448)。

重現：進入 `deal` 決策 → 玩家断線 → 新連線恢復同名玩家 → 新連線提交決策。

結果：玩家 id 已換新，但 `decisionPhase.playerId` 仍是舊 id；新提交沒有收到 `decisionSubmitted`。原本接收選擇的監聽器綁在舊 socket，斷線時被移除。主持人可能仍能以預設結果收束，但玩家不能正常完成原選擇。

建議：使用獨立且穩定的 playerId，不以 socketId 當角色身分。決策保存於房間狀態，重新連線時重新發送卡片／規劃內容，提交以 phaseId 與玩家身分核對。同步檢查拍賣、借貸、舞台參與者及最後一輪的角色引用。

補充程式風險（未另做終局實測）：`playerJoin` 的重連路徑會替換 `finalRoundPendingPlayerIds`，`playerRejoin` 卻未替換；同一種重連目前有兩套不一致邏輯。

### 3. P1｜玩家借款還清，出借方卻沒收到錢，且債權仍存在

位置：[socketServer.ts:3329](/Users/lipeter/Documents/money-game/socketServer.ts:3329)、[gameLogic.ts:686](/Users/lipeter/Documents/money-game/gameLogic.ts:686)。

本機實測：借款 1,000、月利率 1%，再全額還款。

- 借款方：債務已移除。
- 出借方：還款前後現金皆為 46,999，沒有收到本金。
- 出借方：仍持有價值 1,000、月現金流 10 的同筆債權資產。

影響：同一筆債務在兩方帳上不一致，會影響發薪、淨資產、進圈判定與復盤可信度。

建議：以共用 loanId 管理借貸雙方，還款同時轉移現金、更新債權與負債，清償後停止利息。部分還款、債權出售、借貸任一方死亡／斷線，也要有明確一致的規則。不能只修借款方畫面。

### 4. P1｜出借邀請接受負數金額

位置：[socketServer.ts:3922](/Users/lipeter/Documents/money-game/socketServer.ts:3922)。

本機實測：出借邀請金額 -1,000 被接受，產生 `totalDebt: -1000`、`monthlyPayment: -10`。

影響：負債與支出的計算方向顛倒，會污染整個財務模型。這次是 API 層測試，不表示一般玩家必然會從正常按鈕輸入這個值；但伺服器不能信任前端防呆。

建議：統一金融金額為正的有限安全整數，利率為有限且在範圍內的數字；邀請建立與接受時都檢查身份、現金、信用額度及場次狀態。兩種借貸入口目前的驗證強度不同，應合併共用規則。

### 5. P1｜主持人暫停不等於伺服器禁止操作

位置：[socketServer.ts:3245](/Users/lipeter/Documents/money-game/socketServer.ts:3245)。

本機實測：`isPaused: true` 時投資 1 元成功，現金從 48,000 變為 47,999。

影響：畫面雖然隱藏操作，伺服器仍接受投資。其他財務入口也應逐一確認是否允許在別人回合、事件討論或遊戲結束後改動；本次直接重現的是暫停中的投資，不將未逐項重現的入口都算作確認失敗。

建議：後端共用行動授權函式，依「遊戲階段、目前玩家、主持人決策、舞台、允許的行動」判斷。注意決策本身常會暫停時鐘，因此不能粗暴地把所有 `isPaused` 請求全部拒絕；應明確保留當前合法的提交和拍賣行為。

### 6. P1｜離線角色能被別人冒名接手

位置：[socketServer.ts:5335](/Users/lipeter/Documents/money-game/socketServer.ts:5335)。

本機實測：原玩家斷線後，全新連線只提供公開名字與房間代碼，即收到 `rejoinSuccess` 並繼承角色。不需原裝置憑證。

建議：首次加入產生專用重連 token，只回傳給該玩家並保存於其裝置；不同裝置接管需主持人核准。公開房號與名字只供識別，不應當作身分驗證。

### 7. P2｜開局後可直接插入新角色

位置：[socketServer.ts:2443](/Users/lipeter/Documents/money-game/socketServer.ts:2443)。

本機實測：`RatRace` 已開始，仍可新增「中途新玩家」；目前只擋 GameOver。

影響：會改變輪次名單，並引入未走完角色設定的玩家。是否允許晚到是產品選擇，但不宜直接無條件插入。

建議：開局後鎖定新玩家；保留原玩家重連。若要支援晚到，改成主持人核准、補齊角色設定，並從明確的下一輪加入，不改變正在結算的名單。

## 二、需決定的設計風險

### 主持人方便登入與權限安全

目前前後端皆使用通用 `123`，且房間清單可查。這是先前為方便主持人重新登入所作的設計，不是這次新發現的實作偏差，但不適合把它當作真正的主持人權限隔離。

建議保留便利操作，改為「建立房間後記住主持人身分＋手機配對 QR」，另提供主持人救援碼；玩家房號與主持人接管憑證分開。正式公開活動前優先處理。

### 不永久保存復盤，不代表不能做當場故障恢復

目前 room state 在記憶體中。建議增加短期、自動到期的當場 checkpoint，只用於服務重啟恢復，遊戲結束後依規則刪除；永久經典場次仍由主持人選擇保存。是否採用此暫存方案需要明確告知使用者，不應偷偷改為永久留存。

## 三、可優化項目

1. **手機載入量**：目前建置有 1,135.10 kB 的單一 JS chunk（gzip 377.15 kB）。先量測各入口實際載入，再考慮 ECharts 按需載入、復盤模組延後載入；不要只為消除警告調高警告門檻。
2. **保留 PNG 美術**：正在使用的兩張 PNG 各約 3.2–3.3 MB。可以做無損壓縮、確認快取策略、提前載入即將切換的外圈地圖，不必改成向量圖，也不必縮短動畫。
3. **主持人故障提示**：增加目前誰離線、哪個決策等待恢复、哪個選擇已提交的明確提示，將「跳過」與「等待重連」區分；避免顯示重連成功卻不能繼續。
4. **經典保存防呆**：`saveClassicReview` 直接呼叫 localStorage 寫入；儲存空間不足／禁用儲存時需捕捉例外並顯示失敗訊息，避免誤認已保存。這是程式檢查發現的風險，本次未做瀏覽器配額故障實測。
5. **補足驗收範圍**：既有 8 項測試不能代表所有金融、重連及跨圈組合都正確。本次發現的情境應納入持續回歸測試。

## 四、建議修復與驗收順序

1. 輸入驗證與錯誤隔離，確保單一無效操作不會中斷所有房間。
2. 穩定角色身份、重連憑證、決策恢復，統一兩種重連路徑。
3. 借貸雙邊帳務與金額驗證，檢查還款、部分還款、利息與死亡後處理。
4. 主持人控場權限與後端行動白名單；鎖定開局後玩家加入。
5. 在獨立測試環境跑完 4–6 人、20 輪、全體發薪、至少一人進外圈、事件到期、婚姻／生育、斷線、最後一輪、復盤與選擇保存。
6. 最後做真實手機與大螢幕排版、弱網及效能檢查，再評估下一次部署。

驗收要確認的是「數值與角色選擇在所有人畫面上一致、出錯可恢復」，而不是只有後端存活或建置成功。
